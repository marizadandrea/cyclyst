# Cyclyst.Tests.Unit
This project contains the Testing logic with in-memory code strings.
This folder should contain a TestCode directory where you store .txt files containing C# code snippets that the engine must parse.
